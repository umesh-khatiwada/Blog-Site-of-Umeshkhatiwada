declare module '@tailwindcss/typography';

// Mobile optimization utility types (for future use)
// Example: ResponsiveProps<T> for mobile-first responsive design
export type ResponsiveProps<T> = T | { base?: T; sm?: T; md?: T; lg?: T; xl?: T; };
